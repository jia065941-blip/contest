# 数据单元

一条数据单元代表一次固定组合的仿真：`场景 × 红方基线 × 蓝方基线 × 随机种子`。

记录字段：场景、难度、红基线、蓝基线、种子、步数、运行耗时、蓝方计分目标存活/毁伤数、红方存活/损失数、拦截弹发射数、得分、胜负、结果路径、视频路径、状态。

统计口径：同一组合至少以多个种子运行；结果按场景和基线分组，统计均值、标准差、胜率、资源消耗和目标毁伤率。未完成核心适配的组合必须记为“待运行”，不得填入分数。
